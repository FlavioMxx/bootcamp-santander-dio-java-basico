package edu.flaviomxx.anatomiadeclasses;

public class Metodos {

    // Nunca iniciar seu nome com número;
    // Não ter espaços;
    // Não se deve usar acentos e/ou espaços ao criar o nome da variável;
    // Nome de metodo sempre inicia com letra minuscula mas em casos compostos deve ser maiúscula a partir de sua segunda 'palavra';

    //Estrutura
    // TipoDeRetorno NomeObjetivoNoInfinitivo(Parâmetro) { corpo/ações }
    int somar(int numero1, int numero2) {
        return numero1 + numero2;
    }

    String formatarCep(Long cep) {
        return "";
    }
}